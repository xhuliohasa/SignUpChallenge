import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  textInput: {
    borderWidth: 1,
    borderColor: '#d4d4d4',
    borderRadius: 10,
    paddingHorizontal: 15,
    height: 50,
  },
});
